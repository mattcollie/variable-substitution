{
  "var1": "${{ VISIBLE_VARIABLE }}",
  "secret1": "${{ HIDDEN_SECRET_KEY }}",
  "env1": "${{ ACTION_ENV }}",
  "object": {
    "var1": "${{ VISIBLE_VARIABLE }}",
    "secret1": "${{ HIDDEN_SECRET_KEY }}",
    "env1": "${{ ACTION_ENV }}"
  },
  "array": [
    {
      "var1": "${{ VISIBLE_VARIABLE }}",
      "secret1": "${{ HIDDEN_SECRET_KEY }}",
      "env1": "${{ ACTION_ENV }}"
    }
  ]
}